{
    "artifactStoreServiceConfiguration": {
        "certificateName": "artifactstore-cert",
        "endpoint": "$config(artifactStoreEndpoint)"
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Log": "Trace",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "allowListedCertificate": {
        "allowListedDataPlaneSubjectNames": [
            "$config(gatewaySubjectName)"
        ],
        "allowListedControlPlaneSubjectNames": [
            "$config(rpSubjectName)"
        ],
        "allowListedIssuerNames": [
            "AME"
        ]
    },
    "apiServiceCertificateSet": {
        "commonKeyVaultUri": "https://$config(serviceShortName)-$config(pdg.environment.shortName)-$config(regionShortName)-kv.$config(keyVault.domainNameSuffix)",
        "refreshRate": "00:10:00"
    },
    "environment": {
        "environment": "$config(pdg.environment.name)",
        "location": "$config(regionName)",
        "permittedApiVersions": [
            "2023-10-01-preview"
        ]
    },
    "geneva": {
        "metricsAccount": "PurviewDataGovTest",
        "metricsNamespace": "ActiveGlossaryTest",
        "genevaServicesHost": "geneva-services",
        "genevaServicesPort": "8125",
        "defaultDimensions": {
            "Environment": "Dogfood",
            "RoleLocation": "westus2"
        }
    },
    "jobManagerConfiguration": {
        "storageAccountName": "$config(serviceShortName)$config(pdg.environment.shortName)$config(regionShortName)jobs"
    },
    "service": {
        "apiServicePort": 8080,
        "workerServicePort": 8080,
        "apiServiceReadinessProbePort": "$config(dgh.apiServiceReadinessProbePort)",
        "workerServiceReadinessProbePort": "$config(dgh.workerServiceReadinessProbePort)",
        "readinessProbePath": "/healthz/ready"
    },
    "powerBIAuth": {
        "Authority": "https://login.microsoftonline.com/$config(powerBiTenantId)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "$config(azureEnvironment)",
        "ClientId": "$config(powerBiClientId)",
        "RenewBeforeExpiryInMinutes": 2,
        "CertificateName": "client-cert",
        "Resource": "https://analysis.windows.net/powerbi/api",
        "ClientUri": "https://api.powerbi.com",
        "Enabled": "$config(powerBiEnabled)"
    },
    "serverlessPoolAuth": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(synapseRegion)",
        "AzureEnvironment": "$config(azureEnvironment)",
        "RenewBeforeExpiryInMinutes": 2,
        "CertificateName": "client-cert",
        "Resource": "https://database.windows.net",
        "ClientUri": "https://dev.azuresynapse.net",
        "Enabled": "$config(serverlessPoolEnabled)"
    },
    "serverlessPool": {
        "DevelopmentEndpoint": "https://$config(dgh.synapseWorkspaceName).dev.azuresynapse.net",
        "SqlEndpoint": "$config(dgh.synapseWorkspaceName)-ondemand.sql.azuresynapse.net",
        "Database": "dgh$config(pdg.environment.name)db",
        "StatisticsEnabled": true,
        "MinPoolSize": 5,
        "MaxPoolSize": 500,
        "storageAccount": "$config(dgh.synapseWorkspaceName)"
    },
    "synapseSpark": {
        "Workspace": "$config(dgh.synapseWorkspaceName)",
        "AzureRegion": "$config(synapseRegion)",
        "ResourceGroup": "$config(resourceGroupName)",
        "SubscriptionId": "$subscriptionId()"
    },
    "keyVault": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "$config(azureEnvironment)",
        "BaseUrl": "https://$config(serviceShortName)-$config(pdg.environment.shortName)-$config(regionShortName)-kv.$config(keyVault.domainNameSuffix)",
        "Resource": "https://vault.azure.net"
    },
    "auxStorage": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "$config(azureEnvironment)",
        "Resource": "https://management.azure.com",
        "BlobStorageResource": "https://storage.azure.com",
        "AccountName": "$config(dgh.commonStorageAccountName)",
        "ResourceGroup": "$config(resourceGroupName)",
        "SubscriptionId": "$subscriptionId()",
        "EndpointSuffix": "core.windows.net"
    },
    "metadataService": {
        "certificateName": "client-cert",
        "endpoint": "https://$config(metadataServiceEndpoint):8218/",
        "apiVersion": "2019-11-01-preview"
    },
    "exposureControl": {
        "CertificateName": "client-cert",
        "Enabled": true,
        "EnvironmentName": "$config(exposureControlEnvironment)",
        "ClientId": "$config(dgFPAClientId)",
        "CacheRefreshIntervalInMinutes": 15,
        "LoggingLevel": "Warning",
        "UseEstsr": "$config(useEstsr)"
    },
    "partner": {
        "certificateName": "client-cert",
        "enabledPartners": "Catalog;DataAccess;Copilot",
        "collection": "[{ 'name': 'Copilot', 'endpoint': 'https://$config(copilotServiceEndpoint)/controlplane/account', 'validateResponse': 'true', 'dependsOn':[]},{ 'name': 'Catalog', 'endpoint': 'https://$config(catalogServiceEndpoint)/controlplane/account', 'validateResponse': 'true', 'dependsOn':[]},{ 'name': 'DataAccess', 'endpoint': 'https://$config(dataAccessServiceEndpoint)/controlplane/account', 'validateResponse': 'true', 'dependsOn':[]}]"
    },
    "processingStorageAuth": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "",
        "Resource": "https://management.azure.com"
    },
    "processingStorage": {
        "SubscriptionId": "$config(pdg.regionSharedInfrastructure.sharedSubscriptions.storage[0].id)",
        "ResourceGroupName": "$config(processingStorage.resourceGroupName)",
        "AzureRegion": "$config(regionName)",
        "StorageNamePrefix": "dgprocessing$config(dgh.shortRegionName)"
    },
    "eventhubConfiguration": {
        "authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "dataCatalogEventHubName": "catalogevent",
        "dataCatalogCheckpointContainerName": "data-catalog-event-checkpoints",
        "dataAccessEventHubName": "dataaccessevent",
        "dataAccessCheckpointContainerName": "data-access-event-checkpoints",
        "dataQualityEventHubName": "dataqualityevent",
        "dataQualityCheckpointContainerName": "data-quality-event-checkpoints",
        "eventHubNamespace": "$config(catalog.eventHubNamespaceName).servicebus.windows.net",
        "consumerGroup": "dg-health",
        "maxEventsToProcess": 10000
    },
    "accountStorageTable": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "",
        "Resource": "https://management.azure.com",
        "TableName": "$config(processingStorage.tableName)",
        "TableServiceUri": "https://$config(processingStorage.accountName).table.core.windows.net"
    },
    "sparkPoolTable": {
        "Authority": "https://login.microsoftonline.com/$config(pdg.tenant.id)",
        "AzureRegion": "$config(regionName)",
        "AzureEnvironment": "",
        "Resource": "https://management.azure.com",
        "TableName": "$config(dgh.sparkPoolTableName)",
        "TableServiceUri": "https://$config(dgh.commonStorageAccountName).table.core.windows.net"
    }
}
